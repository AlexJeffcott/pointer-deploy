## What this is for

<!-- What a person can do after this that they could not do before, and why that
     is worth doing. Product, not mechanism. If the answer is "nothing a person
     can do", say so and name what it is for instead - a limit closed, a record
     kept, a reading that was not available. -->

## What changes on the page

<!--REVIEW: run `bun run pr` and it replaces this line with the two review URLs,
     the composition each one serves, and one row per view: what qa serves now,
     beside what this branch serves. Do not write them by hand - a screenshot
     filed under the wrong composition is the error the shooter refuses. -->

## What a reviewer should look at

<!-- Name the thing in the picture. "The list reorders when a task moves" beats
     "see the screenshots". Where the two pictures are identical, say so and say
     why the change is worth making anyway. -->

## What is not covered

<!-- What this leaves open, what it refuses to do and why, and any lead a
     reading refuted. A blank here claims that nothing is open. -->

---

| Check | |
| --- | --- |
| `bun run typecheck` | |
| `bun test` | |
| `bun run verify` | |
| The `.feature` file this writes | |
| `TODO.md` updated | |
