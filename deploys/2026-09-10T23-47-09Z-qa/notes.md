The rollback verified again, after the composition rule moved into a tested function.

`--app hello=3bba892b` from a tree that builds no sub-app. Nothing moved; what is being verified is
that the command runs at all, and that it still runs once `composedFrom` rather than `UNITS` decides
what the composition holds.
