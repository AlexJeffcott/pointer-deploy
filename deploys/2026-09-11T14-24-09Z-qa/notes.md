PLAN.md step 2 with the view note corrected. The shell moved alone: list stayed at 2adce208, which is a shell-only deploy leaving the sub-app where its own deploy left it.

Written by hand. The first line is the entry in the changelog, so it says what
this promote demonstrates rather than what it changed - the ids beside it already
say what changed.
