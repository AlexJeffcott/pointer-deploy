A cold read of step 1, and the one thing in it a visitor can see.

The tag box is the change on the page. Before this promote a visitor could not
type a second tag: the input's value was drawn from the store on every keystroke,
`tagsFrom` dropped the empty field after a trailing comma, and Preact wrote
`travel` back over `travel,` as the comma was typed. The scenario that covers
tagging was green throughout, because `page.fill` sets the whole string in one
event and never visits the states a person types through. The panel keeps the
in-progress text as its own state now, the store is still written on every
keystroke, and both tagging scenarios drive the box one key at a time.

Everything else this promote carries is scripts and documents, and the pictures
show none of it: `--drop` on a unit this tree builds no longer crashes,
`membersIn` can see an exported `const` so the shell provides 33 members rather
than 32, `readData` stopped parsing a body it keeps nothing from, and both
contract records name the commit that actually holds their surface.

What the pictures show is what step 1's record showed, with one difference: `/`
draws the panel, `/board` and `/week` still say no unit is placed there yet, and
`/service` and `/backup` are drawn by the frame. Four of the five views fetch
nothing.

`prod` is NOT this composition and cannot be until somebody drops `hello`. It
carries `hello 3bba892b`, which uses four members this surface removed, so every
promote naming this shell on `prod` is refused by name. The runbook is in
`PLAN.md`; TODO §39 is the item.

Written by hand. The first line is the entry in the changelog, so it says what
this promote demonstrates rather than what it changed - the ids beside it already
say what changed.
