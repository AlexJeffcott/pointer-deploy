PLAN.md step 2: the planner is in IndexedDB. A reload keeps the tasks; a second browser starts empty. The note at the foot of the panel is now one of two sentences, chosen by what the frame reports.

Written by hand. The first line is the entry in the changelog, so it says what
this promote demonstrates rather than what it changed - the ids beside it already
say what changed.
